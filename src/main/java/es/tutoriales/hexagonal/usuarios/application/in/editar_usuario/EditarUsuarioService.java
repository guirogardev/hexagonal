package es.tutoriales.hexagonal.usuarios.application.in.editar_usuario;

public class EditarUsuarioService implements EditarUsuarioUseCase {
    @Override
    public void ejecutar(EditarUsuarioCommand command) {
//        Usuario usuario = usuarioRepository
//                .buscarPorId(command.usuarioId())
//                .orElseThrow();
//
//        usuario.cambiarNombre(command.nombre());
//        usuario.cambiarEmail(command.email());
//
//        usuarioRepository.guardar(usuario);
    }
}
